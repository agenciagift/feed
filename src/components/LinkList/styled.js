import styled from "styled-components";
import { EXTRA_LARGE } from "../../constants/sizes";

export const LinkListWrapper = styled.ul`
    list-style: none;
    margin: ${EXTRA_LARGE} 0;
    padding: 0;
`;